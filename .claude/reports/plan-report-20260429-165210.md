---
po_plan_version: "0.1"
name: "debug-cwd-test"
cwd: "../.."

tasks:
  - id: debug-cwd
    agent: general-purpose
    read_only: false
    prompt: |
      以下のコマンドを順番に Bash で実行して、結果をすべて出力してください:
      1. `pwd`
      2. `git rev-parse --show-toplevel`
      3. `git branch --show-current`
      4. `git status --short`
      5. `ls .claude/ 2>/dev/null || echo ".claude/ not found"`
      6. `echo "PO_WORKTREE_GUARD=$PO_WORKTREE_GUARD"`
      7. `echo "test file" > example_test.txt && git add example_test.txt && git commit -m "debug: test commit" && echo "COMMIT SUCCESS"`
---

# デバッグ用タスク

CWD と git の状態を確認するためのデバッグ実行。
