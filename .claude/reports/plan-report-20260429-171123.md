---
po_plan_version: "0.1"
name: "debug-commit-test"
cwd: "../.."

tasks:
  - id: debug-commit
    agent: general-purpose
    read_only: false
    prompt: |
      以下の手順を順番に実行してください。各ステップで結果を報告してください。

      1. `pwd` を実行して現在のディレクトリを確認する
      2. `git branch --show-current` を実行してブランチを確認する
      3. `git status` を実行してgitの状態を確認する
      4. Write ツールで `example/src/addition.py` に以下の内容を作成する:
         ```python
         def add(a, b):
             return a + b
         ```
      5. Write ツールで `example/tests/test_addition.py` に以下の内容を作成する:
         ```python
         from example.src.addition import add
         def test_add():
             assert add(1, 2) == 3
         ```
      6. `git status` を実行してファイルが認識されているか確認する
      7. `git add example/` を実行する
      8. `git status` で staged ファイルを確認する
      9. `git commit -m "TDD: implement add function"` を実行する
      10. `git log --oneline -3` で コミットを確認する
---
