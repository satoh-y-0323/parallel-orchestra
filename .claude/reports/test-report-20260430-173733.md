# Test Report — tdd-addition

- **対象タスク**: `tdd-addition`
- **実装対象**: `example/src/addition.py` の `add(a, b)` 関数
- **テストファイル**: `example/tests/test_addition.py`
- **実行日時**: 2026-04-30 17:37:33
- **TDD フェーズ**: Red（実装未完了。プロダクションコードは `NotImplementedError` を送出）

## 実行コマンド

```
python -m pytest example/tests/test_addition.py -v --rootdir=example
```

## サマリー

| 項目 | 件数 |
|---|---|
| 合格（passed） | 0 |
| 不合格（failed） | 6 |
| スキップ（skipped） | 0 |
| 合計 | 6 |

実行時間: 0.06s / pytest 9.0.3 / Python 3.11.9 / win32

## 不合格テスト一覧

| # | テスト | エラー種別 | 概要 |
|---|---|---|---|
| 1 | `test_add_returns_sum[1-2-3]` | `NotImplementedError` | `add(1, 2)` が NotImplementedError を送出 |
| 2 | `test_add_returns_sum[0-0-0]` | `NotImplementedError` | `add(0, 0)` が NotImplementedError を送出 |
| 3 | `test_add_returns_sum[-1-1-0]` | `NotImplementedError` | `add(-1, 1)` が NotImplementedError を送出 |
| 4 | `test_add_returns_sum[-3--4--7]` | `NotImplementedError` | `add(-3, -4)` が NotImplementedError を送出 |
| 5 | `test_add_returns_sum[100-200-300]` | `NotImplementedError` | `add(100, 200)` が NotImplementedError を送出 |
| 6 | `test_add_with_floats` | `NotImplementedError` | `add(0.1, 0.2)` が NotImplementedError を送出 |

## 不合格テスト詳細（pytest 出力抜粋）

### 1. test_add_returns_sum[1-2-3]
```
example/tests/test_addition.py:15: in test_add_returns_sum
    assert add(a, b) == expected
           ^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

### 2. test_add_returns_sum[0-0-0]
```
example/tests/test_addition.py:15: in test_add_returns_sum
    assert add(a, b) == expected
           ^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

### 3. test_add_returns_sum[-1-1-0]
```
example/tests/test_addition.py:15: in test_add_returns_sum
    assert add(a, b) == expected
           ^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

### 4. test_add_returns_sum[-3--4--7]
```
example/tests/test_addition.py:15: in test_add_returns_sum
    assert add(a, b) == expected
           ^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

### 5. test_add_returns_sum[100-200-300]
```
example/tests/test_addition.py:15: in test_add_returns_sum
    assert add(a, b) == expected
           ^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

### 6. test_add_with_floats
```
example/tests/test_addition.py:19: in test_add_with_floats
    assert add(0.1, 0.2) == pytest.approx(0.3)
           ^^^^^^^^^^^^^
example/src/addition.py:6: in add
    raise NotImplementedError
E   NotImplementedError
```

## テスト仕様（既存テスト）

1. `test_add_returns_sum` — 整数加算を 5 パターン parametrize で検証
   - (1,2)->3, (0,0)->0, (-1,1)->0, (-3,-4)->-7, (100,200)->300
2. `test_add_with_floats` — 浮動小数点加算を `pytest.approx` で検証
   - add(0.1, 0.2) が 0.3 に近似一致

## 次フェーズへの引き継ぎ（Green フェーズ向け）

- `example/src/addition.py` の `add(a, b)` を `return a + b` で実装すれば全 6 テストが合格する見込み。
- 関数シグネチャ `def add(a: Union[int, float], b: Union[int, float]) -> Union[int, float]` は既に定義済み。
- 浮動小数点誤差は `pytest.approx` で吸収済み。

## Tester ペルソナの遵守事項

- プロダクションコード（`example/src/addition.py`）は編集していない。
- テストコードは既存のものをそのまま使用し、新規作成・変更は行っていない。
