# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
build/
dist/
.eggs/
*.egg
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# parallel-orchestra isolated worktrees
.po-worktrees/

# parallel-orchestra run-state files (contain absolute paths and task IDs)
.po-run-state-*.json

# Repository-root .claude/ is reserved as the user's local Claude Code
# workspace (agent definitions, skills, hooks, harness settings, etc.) and
# is intentionally not tracked here. The PO-specific demo lives under
# example/.claude/ instead. Pair parallel-orchestra with whatever framework
# (or no framework) you like in your own .claude/ — its contents stay
# private to your machine.
/.claude/

# Environment variables
.env
.env.*

# Examples / local testing artifacts (not committed)
examples/
